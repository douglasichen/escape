//change the jump height back to its original value, because this alarm is set when the player colides with the jump powerup

powerup_jump = false

jump_height = initial_jump_height 

jump_height_max = initial_jump_height_max
